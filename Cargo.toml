[workspace]
resolver = "3"
members = ["kernel", "boot", "userspace/hello", "abi"]

[workspace.package]
edition = "2024"
rust-version = "1.85"
license = "MIT"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
